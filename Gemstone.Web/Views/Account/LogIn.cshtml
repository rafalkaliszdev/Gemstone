@model Gemstone.Web.ViewModels.AccountModel
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor accessor
@{
    ViewData["PageTitle"] = "Sign In";
}

@if (ViewData["unauthorized"] != null && (bool)ViewData["unauthorized"])
{
    <div class="alert alert-warning">
        <strong>Unauthorized!</strong> Log in to access restricted resource.
    </div>
}

@* todo test this form *@


<th:form asp-controller="Account" asp-action="LogIn" method="post">
    <th:div asp-validation-summary="ModelOnly" class="text-danger"></th:div>
    <div class="form-group">
        <th:label asp-for="Username" class="control-label"></th:label>
        <th:input asp-for="Username" class="form-control" />
        <th:span asp-validation-for="Username" class="text-danger"></th:span>
    </div>
    <div class="form-group">
        <th:label asp-for="Password" class="control-label"></th:label>
        <th:input asp-for="Password" class="form-control" />
        <th:span asp-validation-for="Password" class="text-danger"></th:span>
    </div>



    <div class="form-group">
        <input type="submit" value="Log in" class="btn btn-primary" />
    </div>
    
    
    
    <input type="submit" value="Log in" class="btn btn-primary" />
</th:form>
